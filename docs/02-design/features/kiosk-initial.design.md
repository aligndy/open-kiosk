# Kiosk Initial Implementation Design Document

> **Summary**: 카페 키오스크 웹 앱의 관리/구매/결제Mock/다국어/AI이미지 기능 상세 설계
>
> **Project**: k (카페 키오스크)
> **Version**: 0.1.0
> **Author**: dgoon
> **Date**: 2026-02-28
> **Status**: Draft
> **Planning Doc**: [kiosk-initial.plan.md](../01-plan/features/kiosk-initial.plan.md)

---

## 1. Overview

### 1.1 Design Goals

- 디지털 약자가 3단계 이내에 주문을 완료할 수 있는 직관적 구매 UI
- 관리자가 메뉴/카테고리를 쉽게 등록하고, AI로 이미지 생성 및 다국어 번역이 가능한 관리 UI
- Next.js App Router + SQLite + Prisma 기반 단일 서버 풀스택 구조
- 결제는 Mock으로 서명 팝업 처리

### 1.2 Design Principles

- **단순성 우선**: 최소한의 선택 단계, 큰 터치 영역, 큰 글씨
- **관심사 분리**: API Routes(서버) ↔ 클라이언트 컴포넌트 ↔ Zustand Store 명확 분리
- **점진적 확장**: Mock 결제 → 실제 토스 프론트 연동으로 교체 가능한 구조

---

## 2. Architecture

### 2.1 Component Diagram

```
┌──────────────────────────────────────────────────────┐
│                     Browser                           │
│  ┌─────────────┐  ┌──────────────┐  ┌─────────────┐ │
│  │  /shop      │  │  /admin      │  │ /tossfront  │ │
│  │  (구매 UI)   │  │  (관리 UI)   │  │ (폴링 대상)  │ │
│  └──────┬──────┘  └──────┬───────┘  └──────┬──────┘ │
│         │                │                  │        │
│  ┌──────┴──────┐  ┌──────┴───────┐         │        │
│  │ Zustand     │  │ React State  │         │        │
│  │ (cart store)│  │ (form state) │         │        │
│  └──────┬──────┘  └──────┬───────┘         │        │
└─────────┼────────────────┼─────────────────┼────────┘
          │                │                  │
          ▼                ▼                  ▼
┌──────────────────────────────────────────────────────┐
│              Next.js API Routes                       │
│  ┌────────┐ ┌────────┐ ┌──────────┐ ┌────────────┐  │
│  │/api/   │ │/api/   │ │/api/     │ │/api/       │  │
│  │categori│ │menus   │ │orders    │ │translate   │  │
│  │es      │ │        │ │          │ │generate-img│  │
│  └───┬────┘ └───┬────┘ └────┬─────┘ └─────┬──────┘  │
│      │          │           │              │         │
│      ▼          ▼           ▼              ▼         │
│  ┌──────────────────┐  ┌──────────────────────┐      │
│  │  Prisma Client   │  │  Gemini API Client   │      │
│  │  (SQLite)        │  │  (번역 + 이미지생성)   │      │
│  └──────────────────┘  └──────────────────────┘      │
└──────────────────────────────────────────────────────┘
```

### 2.2 Data Flow

```
[구매 플로우]
메뉴 조회: Browser → GET /api/menus → Prisma → SQLite → Response → Render

장바구니: 클라이언트 Zustand Store (서버 미전송)

주문 생성: Browser → POST /api/orders → Prisma → SQLite → 주문번호 반환

[얼굴 인식 및 자판기 모드 플로우]
초기 로드: Browser(카메라 권한) → 화면 캡처 → POST /api/estimate-age (FormData) → Gemini Vision API
상태 전환: 연령 >= 기준(50) 판별 시 → 클라이언트 VendingMode=true 상태 변경 → 자판기 모드 UI 렌더링

[관리 플로우]
메뉴 등록: Browser → POST /api/menus (+ FormData) → Prisma → SQLite
이미지 생성: Browser → POST /api/generate-image → Gemini API → 이미지 저장 → URL 반환
번역: Browser → POST /api/translate → Gemini API → Prisma 업데이트 → Response
```

### 2.3 Dependencies

| Component | Depends On | Purpose |
|-----------|-----------|---------|
| Shop Pages | Zustand Store, API Routes | 메뉴 조회, 장바구니, 주문 |
| Admin Pages | API Routes | 메뉴/카테고리 CRUD, 번역, 이미지 생성 |
| API Routes | Prisma Client | DB 접근 |
| API Routes (translate, generate-image) | Gemini Client | AI 기능 |
| Prisma Client | SQLite | 데이터 저장소 |

---

## 3. Data Model

### 3.1 Prisma Schema

```prisma
datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Category {
  id               Int      @id @default(autoincrement())
  name             String
  nameTranslations String   @default("{}") // JSON: { "en": "Coffee", "ja": "コーヒー" }
  sortOrder        Int      @default(0)
  isActive         Boolean  @default(true)
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
  menus            Menu[]
}

model Menu {
  id                       Int          @id @default(autoincrement())
  categoryId               Int
  category                 Category     @relation(fields: [categoryId], references: [id])
  name                     String
  nameTranslations         String       @default("{}") // JSON
  description              String       @default("")
  descriptionTranslations  String       @default("{}") // JSON
  price                    Int          // 원 단위 정수
  imageUrl                 String?
  isActive                 Boolean      @default(true)
  sortOrder                Int          @default(0)
  createdAt                DateTime     @default(now())
  updatedAt                DateTime     @updatedAt
  optionGroups             OptionGroup[]
  orderItems               OrderItem[]
}

model OptionGroup {
  id                       Int      @id @default(autoincrement())
  menuId                   Int
  menu                     Menu     @relation(fields: [menuId], references: [id], onDelete: Cascade)
  name                     String   // 관리자가 자유롭게 정의 (예: "사이즈", "온도", "50도/뜨거움", "샷 추가", "시럽" 등)
  nameTranslations         String   @default("{}") // JSON
  required                 Boolean  @default(false)
  sortOrder                Int      @default(0)
  options                  Option[]
}

model Option {
  id                 Int         @id @default(autoincrement())
  optionGroupId      Int
  optionGroup        OptionGroup @relation(fields: [optionGroupId], references: [id], onDelete: Cascade)
  name               String     // 관리자가 자유롭게 정의 (예: "Regular", "50도", "샷 1개" 등)
  nameTranslations   String     @default("{}") // JSON
  priceModifier      Int        @default(0) // 추가 금액 (원)
  sortOrder          Int        @default(0)
}

model Order {
  id          Int         @id @default(autoincrement())
  orderNumber String      @unique // "A001" 형식
  status      String      @default("pending") // pending → completed (관리자 완료처리)
  totalAmount Int
  language    String      @default("ko") // 주문 시 사용 언어
  createdAt   DateTime    @default(now())
  items       OrderItem[]
}

model OrderItem {
  id             Int    @id @default(autoincrement())
  orderId        Int
  order          Order  @relation(fields: [orderId], references: [id], onDelete: Cascade)
  menuId         Int
  menu           Menu   @relation(fields: [menuId], references: [id])
  menuName       String // 주문 시점의 메뉴명 스냅샷
  quantity       Int
  unitPrice      Int    // 주문 시점 가격
  selectedOptions String @default("[]") // JSON: [{ group: "사이즈", option: "Large", price: 500 }]
  subtotal       Int
}

model StoreSettings {
  id                    Int     @id @default(1)
  storeName             String  @default("카페")
  defaultLanguage       String  @default("ko")
  supportedLanguages    String  @default("[\"ko\"]") // JSON array
  useCameraDetection    Boolean @default(true)       // 카메라 나이 인식 사용 여부
  vendingModeAge        Int     @default(50)         // 자판기 모드 전환 기준 연령
}
```

### 3.2 Entity Relationships

```
[Category] 1 ──── N [Menu]
                       │
                       ├── 1 ──── N [OptionGroup]
                       │                 │
                       │                 └── 1 ──── N [Option]
                       │
                       └── 1 ──── N [OrderItem]

[Order] 1 ──── N [OrderItem]

[StoreSettings] (Singleton)
```

### 3.3 번역 데이터 저장 방식

번역은 각 모델의 `*Translations` 필드에 JSON 문자열로 저장한다. SQLite는 JSON 타입을 지원하지 않으므로 String 필드에 직렬화하여 저장한다.

```typescript
// 저장 형태
{
  "en": "Americano",
  "ja": "アメリカーノ",
  "zh": "美式咖啡"
}

// 조회 시 파싱
const translations = JSON.parse(menu.nameTranslations);
const localizedName = translations[currentLanguage] || menu.name;
```

---

## 4. API Specification

### 4.1 Endpoint List

| Method | Path | Description | Request Body | Response |
|--------|------|-------------|-------------|----------|
| **Categories** | | | | |
| GET | `/api/categories` | 카테고리 목록 | - | Category[] |
| POST | `/api/categories` | 카테고리 생성 | `{ name, sortOrder? }` | Category |
| PUT | `/api/categories/[id]` | 카테고리 수정 | `{ name?, sortOrder?, isActive? }` | Category |
| DELETE | `/api/categories/[id]` | 카테고리 삭제 | - | `{ success: true }` |
| **Menus** | | | | |
| GET | `/api/menus` | 메뉴 목록 (query: `categoryId?`) | - | Menu[] (with optionGroups) |
| GET | `/api/menus/[id]` | 메뉴 상세 | - | Menu (with optionGroups, options) |
| POST | `/api/menus` | 메뉴 생성 | FormData or JSON | Menu |
| PUT | `/api/menus/[id]` | 메뉴 수정 | FormData or JSON | Menu |
| DELETE | `/api/menus/[id]` | 메뉴 삭제 | - | `{ success: true }` |
| **Option Groups** | | | | |
| POST | `/api/menus/[id]/options` | 옵션그룹+옵션 생성 | `{ name, required?, options[] }` | OptionGroup |
| PUT | `/api/menus/[id]/options/[groupId]` | 옵션그룹 수정 | `{ name?, options[]? }` | OptionGroup |
| DELETE | `/api/menus/[id]/options/[groupId]` | 옵션그룹 삭제 | - | `{ success: true }` |
| **Orders** | | | | |
| POST | `/api/orders` | 주문 생성 | `{ items[], language }` | Order (with orderNumber) |
| GET | `/api/orders` | 주문 목록 (admin용, query: `status?`) | - | Order[] |
| PATCH | `/api/orders/[id]` | 주문 상태 변경 (완료처리) | `{ status }` | Order |
| **AI (Gemini)** | | | | |
| POST | `/api/translate` | 일괄 번역 | `{ targetLanguage }` | `{ success, translatedCount }` |
| POST | `/api/generate-image` | 이미지 생성 | `{ prompt, menuId? }` | `{ imageUrl }` |
| POST | `/api/estimate-age` | 나이 추정 | FormData (image) | `{ estimatedAge, isVendingMode }` |
| **Toss Front (Mock)** | | | | |
| GET | `/api/tossfront` | 폴링 엔드포인트 | - | `{ status, data? }` |
| **Store Settings** | | | | |
| GET | `/api/settings` | 매장 설정 조회 | - | StoreSettings |
| PUT | `/api/settings` | 매장 설정 수정 | `{ storeName?, supportedLanguages? }` | StoreSettings |

### 4.2 주요 API 상세

#### `POST /api/orders`

```typescript
// Request
{
  "items": [
    {
      "menuId": 1,
      "quantity": 2,
      "selectedOptions": [
        { "groupId": 1, "optionId": 3 }
      ]
    }
  ],
  "language": "ko"
}

// Response (201)
{
  "id": 1,
  "orderNumber": "A001",
  "status": "completed",
  "totalAmount": 9800,
  "items": [
    {
      "menuName": "아메리카노",
      "quantity": 2,
      "unitPrice": 4500,
      "selectedOptions": [
        { "group": "사이즈", "option": "Large", "price": 500 }
      ],
      "subtotal": 9800
    }
  ],
  "createdAt": "2026-02-28T10:00:00Z"
}
```

#### `PATCH /api/orders/[id]`

```typescript
// Request
{
  "status": "completed"
}

// Response (200)
{
  "id": 3,
  "orderNumber": "A003",
  "status": "completed",
  "totalAmount": 16500,
  "createdAt": "2026-02-28T14:32:00Z"
}
```

#### `POST /api/translate`

```typescript
// Request
{
  "targetLanguage": "en"  // 번역 대상 언어
}

// Response (200)
{
  "success": true,
  "translatedCount": 15,  // 번역된 항목 수 (카테고리 + 메뉴 + 옵션)
  "language": "en"
}
```

Gemini API 호출 시 프롬프트:
```
다음 카페 메뉴 항목들을 {targetLanguage}로 번역해주세요.
자연스럽고 간결하게 번역하되, 카페 메뉴에 적합한 표현을 사용하세요.
JSON 형식으로 응답해주세요.

입력:
{ "items": [ { "id": 1, "type": "category", "field": "name", "value": "커피" }, ... ] }

출력:
{ "translations": [ { "id": 1, "type": "category", "field": "name", "value": "Coffee" }, ... ] }
```

#### `POST /api/generate-image`

```typescript
// Request
{
  "prompt": "따뜻한 아메리카노, 흰 머그컵에 담긴 검은 커피, 카페 분위기",
  "menuId": 1  // optional: 생성 후 메뉴에 바로 연결
}

// Response (200)
{
  "imageUrl": "/generated/menu-1-1709100000.png"
}
```

Gemini API 호출 시 이미지 생성 프롬프트:
```
카페 메뉴 사진을 생성해주세요. 음식/음료 사진 스타일로, 깨끗한 배경에 메뉴만 부각되도록 합니다.
설명: {prompt}
스타일: 프로페셔널 푸드 포토그래피, 밝은 조명, 깨끗한 배경
```

#### `POST /api/estimate-age`

```typescript
// Request (FormData)
// File: image (카메라 캡처 이미지)

// Response (200)
{
  "estimatedAge": 55,
  "isVendingMode": true // DB의 vendingModeAge(예: 50) 이상일 경우 true 반환
}
```

Gemini Vision API 호출 시 프롬프트:
```
이 사람의 나이대를 숫자로만 추정해서 대답해주세요 (예: 25, 50, 65). 다른 말은 덧붙이지 마세요.
※ 얼굴이 없거나 사람이 아닌 경우 -1을 반환하세요.
```

#### `GET /api/tossfront`

```typescript
// Mock Response (200)
{
  "status": "ready",  // 항상 ready 반환 (Mock)
  "message": "Toss Front Mock endpoint"
}
```

### 4.3 이미지 업로드 처리

메뉴 생성/수정 시 이미지 업로드는 FormData를 통해 처리한다.

```typescript
// POST /api/menus (FormData)
// Fields: name, description, price, categoryId, sortOrder, isActive
// File: image (optional)

// 저장 경로: public/uploads/menu-{id}-{timestamp}.{ext}
// DB 저장: imageUrl = "/uploads/menu-{id}-{timestamp}.{ext}"
```

---

## 5. UI/UX Design

### 5.1 일반 구매 페이지 (`/shop`) - 세로 키오스크 (1080x1920)

우측 하단 등에 '돋보기(큰 글씨 모드)' 수동 토글 버튼을 두어 권한이 없거나 인식이 안된 사용자도 자판기 모드로 전환할 수 있게 한다.

```
┌────────────────────────────────────────┐
│  ☰ 카페이름          [한국어 ▼] [🛒 2] │  ← 헤더: 매장명, 언어 선택, 장바구니
├────────────────────────────────────────┤
│  [커피]  [음료]  [디저트]  [세트]       │  ← 카테고리 탭 (가로 스크롤)
├────────────────────────────────────────┤
│                                        │
│  ┌─────────┐  ┌─────────┐             │
│  │  📷     │  │  📷     │             │
│  │         │  │         │             │
│  │아메리카노│  │카페라떼  │             │  ← 메뉴 그리드 (2열)
│  │ 4,500원 │  │ 5,000원 │             │     큰 이미지 + 큰 글씨
│  │ [담기]  │  │ [담기]  │             │     담기 버튼 크게
│  └─────────┘  └─────────┘             │
│                                        │
│  ┌─────────┐  ┌─────────┐             │
│  │  📷     │  │  📷     │             │
│  │         │  │         │             │
│  │카푸치노  │  │에스프레소│             │
│  │ 5,500원 │  │ 4,000원 │             │
│  │ [담기]  │  │ [담기]  │             │
│  └─────────┘  └─────────┘             │
│                                        │
├────────────────────────────────────────┤
│  🛒 장바구니 2개  │  합계 9,500원      │  ← 하단 고정 바
│  ┌─────────────────────────────────┐  │
│  │      [주문하기]                  │  │  ← 큰 주문 버튼
│  └─────────────────────────────────┘  │
└────────────────────────────────────────┘
```

### 5.2 자판기 모드 UI (Vending Machine Mode)

50대 이상으로 인식되거나 수동으로 전환된 경우 제공되는 극단적으로 단순화된 UI. 
- 복잡한 메뉴 뎁스 제거
- 가로 1열 또는 2열의 거대한 버튼
- 옵션 팝업 생략 (기본 옵션: 뜨거움/레귤러 사이즈 등으로 즉시 담기)
- 모달 단계 축소 (장바구니 확인 없이 '결제하기' 버튼으로 직행도 고려)

```
┌────────────────────────────────────────┐
│  [일반 화면으로]             [🛒 결제하기] │
├────────────────────────────────────────┤
│                                        │
│  ┌──────────────────────────────────┐  │
│  │                                  │  │
│  │   📷 아메리카노 (따뜻하게)          │  │
│  │   4,500원                         │  │
│  │                                  │  │
│  │   [ 👆 이 음료 선택하기    ]        │  │
│  └──────────────────────────────────┘  │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │                                  │  │
│  │   📷 카페라떼 (따뜻하게)            │  │
│  │   5,000원                         │  │
│  │                                  │  │
│  │   [ 👆 이 음료 선택하기    ]        │  │
│  └──────────────────────────────────┘  │
│                                        │
└────────────────────────────────────────┘
```
**동작 방식**: '이 음료 선택하기'를 누르면 즉시 장바구니에 담기고 하단의 누적 금액이 업데이트되며 결제 유도.

### 5.3 옵션 선택 모달 (일반 모드 메뉴 클릭 시)

> 옵션 그룹은 관리자가 메뉴별로 자유롭게 정의한다.
> 정해진 옵션 종류(사이즈, 온도 등)는 없으며, 메뉴에 등록된
> OptionGroup을 순서대로 동적 렌더링한다.
> 옵션 그룹이 없는 메뉴는 옵션 섹션 없이 수량만 표시된다.

```
┌────────────────────────────────────────┐
│           아메리카노                    │
│           4,500원                       │
│  ┌──────────────────────────────────┐  │
│  │         📷 메뉴 이미지            │  │
│  └──────────────────────────────────┘  │
│                                        │
│  ── 아래는 동적 렌더링 영역 ──         │
│  (메뉴에 등록된 OptionGroup 순서대로)   │
│                                        │
│  {optionGroup.name} {필수 시 *표시}    │
│  ┌──────────┐  ┌──────────┐  ...     │
│  │ {opt.name}│  │ {opt.name}│         │
│  │ {+금액}  │  │ {+금액}  │          │
│  └──────────┘  └──────────┘          │
│                                        │
│  {다음 optionGroup.name}              │
│  ┌──────────┐  ┌──────────┐  ...     │
│  │ {opt.name}│  │ {opt.name}│         │
│  └──────────┘  └──────────┘          │
│  ── 동적 렌더링 영역 끝 ──            │
│                                        │
│  수량  [ - ]  1  [ + ]                 │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │  [장바구니 담기  {계산된 금액}원]  │  │
│  └──────────────────────────────────┘  │
│  ┌──────────────────────────────────┐  │
│  │          [취소]                   │  │
│  └──────────────────────────────────┘  │
└────────────────────────────────────────┘

예시: 관리자가 "온도" 그룹(뜨거움/50도/차가움)과
     "사이즈" 그룹(S/M/L)을 등록한 경우:

│  온도 *                                │
│  ┌────────┐ ┌────────┐ ┌────────┐    │
│  │ 뜨거움  │ │  50도  │ │ 차가움  │    │
│  │  기본   │ │  기본  │ │  기본   │    │
│  └────────┘ └────────┘ └────────┘    │
│                                        │
│  사이즈                                │
│  ┌────────┐ ┌────────┐ ┌────────┐    │
│  │   S    │ │   M    │ │   L    │    │
│  │  기본  │ │ +500원 │ │+1,000원│    │
│  └────────┘ └────────┘ └────────┘    │
```

### 5.3 장바구니 / 주문 확인 화면

```
┌────────────────────────────────────────┐
│  ← 뒤로          주문 확인             │
├────────────────────────────────────────┤
│                                        │
│  1. 아메리카노                         │
│     50도 / L                           │
│     5,500원 × 1          5,500원       │
│                          [삭제]        │
│  ──────────────────────────────────    │
│  2. 카페라떼                           │
│     뜨거움 / M                         │
│     5,500원 × 1          5,500원       │
│                          [삭제]        │
│                                        │
├────────────────────────────────────────┤
│                                        │
│  합계                   10,000원       │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │      [결제하기  10,000원]        │  │
│  └──────────────────────────────────┘  │
│  ┌──────────────────────────────────┐  │
│  │        [메뉴로 돌아가기]          │  │
│  └──────────────────────────────────┘  │
└────────────────────────────────────────┘
```

### 5.4 결제 Mock 서명 팝업

```
┌────────────────────────────────────────┐
│                                        │
│          결제 확인                      │
│                                        │
│     총 결제금액: 10,000원              │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │                                  │  │
│  │    여기에 서명해주세요             │  │
│  │                                  │  │
│  │    ~~~서명 캔버스 영역~~~          │  │
│  │                                  │  │
│  │                                  │  │
│  └──────────────────────────────────┘  │
│                                        │
│  ┌──────────┐  ┌──────────────────┐   │
│  │  다시쓰기 │  │    결제 완료     │   │
│  └──────────┘  └──────────────────┘   │
│  ┌──────────────────────────────────┐  │
│  │          [취소]                   │  │
│  └──────────────────────────────────┘  │
└────────────────────────────────────────┘
```

### 5.5 주문 완료 화면

```
┌────────────────────────────────────────┐
│                                        │
│                                        │
│            ✓ 주문 완료!                │
│                                        │
│         주문번호: A 0 0 1              │
│         (큰 글씨로 표시)                │
│                                        │
│                                        │
│     잠시 후 자동으로 처음으로            │
│     돌아갑니다 (5초)                    │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │        [처음으로]                 │  │
│  └──────────────────────────────────┘  │
└────────────────────────────────────────┘
```

### 5.6 관리 페이지 (`/admin`)

```
┌────────────────────────────────────────────────────┐
│  카페 관리         [미리보기] [설정]                 │
├──────────┬─────────────────────────────────────────┤
│          │                                         │
│ 주문     │  메뉴 관리                              │
│ 카테고리  │  ┌───────────────────────────────────┐  │
│ 메뉴     │  │ [+ 메뉴 추가]                     │  │
│ 번역     │  ├───────────────────────────────────┤  │
│ 설정     │                                         │
│          │  │ ☐ 아메리카노  4,500원  [수정][삭제]│  │
│          │  │ ☐ 카페라떼    5,000원  [수정][삭제]│  │
│          │  │ ☐ 카푸치노    5,500원  [수정][삭제]│  │
│          │  └───────────────────────────────────┘  │
│          │                                         │
└──────────┴─────────────────────────────────────────┘
```

### 5.7 관리 - 메뉴 등록/수정 폼

```
┌────────────────────────────────────────────────────┐
│  ← 뒤로            메뉴 등록                       │
├────────────────────────────────────────────────────┤
│                                                    │
│  카테고리:  [커피 ▼]                               │
│  메뉴명:   [아메리카노        ]                    │
│  설명:     [신선한 원두로 내린 ]                    │
│  가격:     [4500             ] 원                  │
│                                                    │
│  이미지:                                           │
│  ┌─────────────────────────────────────────────┐  │
│  │  📷 이미지 미리보기                          │  │
│  └─────────────────────────────────────────────┘  │
│  [파일 업로드]  [AI 이미지 생성]                    │
│                                                    │
│  AI 이미지 생성 프롬프트: (AI 이미지 생성 클릭 시)  │
│  [따뜻한 아메리카노, 흰 머그컵          ]          │
│  [생성하기]                                        │
│                                                    │
│  옵션 그룹: (관리자가 자유롭게 추가/삭제)            │
│  ┌──────────────────────────────────────────┐     │
│  │ 그룹명: [온도        ]  ☑ 필수  [삭제]   │     │
│  │ ┌────────────────────────────────────┐   │     │
│  │ │ 뜨거움 (+0원) | 50도 (+0원)       │   │     │
│  │ │ 차가움 (+0원)                     │   │     │
│  │ └────────────────────────────────────┘   │     │
│  │ [+ 옵션 항목 추가]                       │     │
│  └──────────────────────────────────────────┘     │
│  ┌──────────────────────────────────────────┐     │
│  │ 그룹명: [사이즈      ]  ☐ 필수  [삭제]   │     │
│  │ ┌────────────────────────────────────┐   │     │
│  │ │ S (+0원) | M (+500원) | L (+1000원)│   │     │
│  │ └────────────────────────────────────┘   │     │
│  │ [+ 옵션 항목 추가]                       │     │
│  └──────────────────────────────────────────┘     │
│  [+ 옵션 그룹 추가]                                │
│                                                    │
│  ※ 그룹명/옵션명은 자유롭게 입력 가능               │
│  ※ 각 옵션에 추가 금액 설정 가능 (0원이면 기본)     │
│                                                    │
│  ┌──────────────┐  ┌──────────────┐               │
│  │    취소      │  │    저장      │               │
│  └──────────────┘  └──────────────┘               │
└────────────────────────────────────────────────────┘
```

### 5.8 관리 - 주문 관리

> 주문이 들어오면 대기(pending) 상태로 목록에 표시된다.
> 관리자가 음료/음식을 준비한 뒤 "완료" 버튼을 눌러 처리한다.

```
┌────────────────────────────────────────────────────┐
│  주문 관리                    [대기 3] [완료 12]    │
├────────────────────────────────────────────────────┤
│                                                    │
│  ┌──────────────────────────────────────────────┐ │
│  │ #A003  14:32                     ⏳ 대기     │ │
│  │  아메리카노 (50도/L) × 1         5,500원     │ │
│  │  카페라떼 (뜨거움/M) × 2        11,000원     │ │
│  │                          합계   16,500원     │ │
│  │                        [완료 처리]           │ │
│  └──────────────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────────┐ │
│  │ #A002  14:28                     ⏳ 대기     │ │
│  │  에스프레소 × 1                  4,000원     │ │
│  │                          합계    4,000원     │ │
│  │                        [완료 처리]           │ │
│  └──────────────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────────┐ │
│  │ #A001  14:15                     ✅ 완료     │ │
│  │  카푸치노 (차가움/S) × 1         5,500원     │ │
│  │                          합계    5,500원     │ │
│  └──────────────────────────────────────────────┘ │
│                                                    │
└────────────────────────────────────────────────────┘
```

### 5.9 관리 - 번역 관리

```
┌────────────────────────────────────────────────────┐
│  번역 관리                                          │
├────────────────────────────────────────────────────┤
│                                                    │
│  대상 언어: [English ▼]    [번역하기]               │
│                                                    │
│  번역 상태:                                        │
│  ┌──────────────────────────────────────────────┐ │
│  │ 항목           │ 한국어     │ English         │ │
│  ├──────────────────────────────────────────────┤ │
│  │ [카테고리] 커피 │ 커피      │ [Coffee    ]    │ │
│  │ [메뉴] 아메리카노│ 아메리카노│ [Americano ]    │ │
│  │ [설명]          │ 신선한... │ [Fresh...  ]    │ │
│  │ [옵션] Regular  │ Regular  │ [Regular   ]    │ │
│  │ [옵션] Large    │ Large    │ [Large     ]    │ │
│  └──────────────────────────────────────────────┘ │
│                                                    │
│  ※ AI 번역 결과를 직접 수정할 수 있습니다            │
│                                                    │
│  [저장]                                            │
└────────────────────────────────────────────────────┘
```

### 5.9 User Flow

```
[구매 플로우]
- /shop 진입 시 카메라 접근 권한 요청 (비동기) → 화면 캡처 및 나이 추정 API 호출
- 연령 >= 50 판별 시 자판기 모드 활성화 (수동으로도 전환 가능)
/shop (일반 or 자판기 모드) → 카테고리 선택 → 메뉴 클릭 → (옵션 선택) → 장바구니 담기
     → 장바구니 확인 → 결제하기 → 서명 팝업 → 결제 완료 → 주문 완료 (5초 후 자동 리셋)

[관리 플로우]
/admin → 사이드바 메뉴 선택
     → 주문 관리: 대기 주문 목록 확인 → 완료 처리
     → 카테고리 관리: 추가/수정/삭제/정렬
     → 메뉴 관리: 추가(이미지 업로드 or AI 생성, 옵션 자유 설정)/수정/삭제
     → 번역 관리: 언어 선택 → 번역하기 → 결과 확인/수정 → 저장
     → 설정: 매장명, 지원 언어 설정
```

### 5.10 Component List

| Component | Location | Responsibility |
|-----------|----------|----------------|
| `ShopLayout` | `src/app/shop/layout.tsx` | 구매 페이지 레이아웃 (헤더, 하단바) |
| `CategoryTabs` | `src/components/shop/CategoryTabs.tsx` | 카테고리 탭 네비게이션 |
| `MenuGrid` | `src/components/shop/MenuGrid.tsx` | 메뉴 카드 그리드 (2열) |
| `MenuCard` | `src/components/shop/MenuCard.tsx` | 개별 메뉴 카드 (이미지, 이름, 가격, 담기) |
| `OptionModal` | `src/components/shop/OptionModal.tsx` | 옵션 선택 모달 |
| `CartBar` | `src/components/shop/CartBar.tsx` | 하단 장바구니 고정 바 |
| `CartView` | `src/components/shop/CartView.tsx` | 장바구니 상세 (수량 변경, 삭제) |
| `PaymentModal` | `src/components/shop/PaymentModal.tsx` | Mock 결제 서명 팝업 |
| `SignaturePad` | `src/components/shop/SignaturePad.tsx` | 서명 캔버스 |
| `OrderComplete` | `src/components/shop/OrderComplete.tsx` | 주문 완료 화면 |
| `LanguageSelector` | `src/components/shop/LanguageSelector.tsx` | 언어 선택 드롭다운 |
| `CameraAgeDetector` | `src/components/shop/CameraAgeDetector.tsx` | 숨겨진 비디오 캡처 및 연령 추정 클라이언트 |
| `VendingMenuCard` | `src/components/shop/VendingMenuCard.tsx` | 자판기 모드용 대형 카드 컴포넌트 |
| `AdminLayout` | `src/app/admin/layout.tsx` | 관리 페이지 레이아웃 (사이드바) |
| `AdminSidebar` | `src/components/admin/AdminSidebar.tsx` | 관리 사이드바 네비게이션 |
| `OrderManager` | `src/components/admin/OrderManager.tsx` | 주문 목록 및 완료 처리 |
| `OrderCard` | `src/components/admin/OrderCard.tsx` | 개별 주문 카드 (항목, 완료 버튼) |
| `CategoryManager` | `src/components/admin/CategoryManager.tsx` | 카테고리 CRUD UI |
| `MenuManager` | `src/components/admin/MenuManager.tsx` | 메뉴 목록 관리 |
| `MenuForm` | `src/components/admin/MenuForm.tsx` | 메뉴 등록/수정 폼 |
| `ImageUploader` | `src/components/admin/ImageUploader.tsx` | 이미지 업로드 |
| `ImageGenerator` | `src/components/admin/ImageGenerator.tsx` | AI 이미지 생성 UI |
| `TranslationManager` | `src/components/admin/TranslationManager.tsx` | 번역 관리 UI |
| `SettingsForm` | `src/components/admin/SettingsForm.tsx` | 매장 설정 폼 |

---

## 6. State Management

### 6.1 Zustand Stores

```typescript
// src/stores/cartStore.ts
interface CartItem {
  menuId: number;
  menuName: string;
  imageUrl: string | null;
  quantity: number;
  unitPrice: number;
  selectedOptions: {
    groupName: string;
    optionName: string;
    priceModifier: number;
  }[];
  subtotal: number;
}

interface CartStore {
  items: CartItem[];
  addItem: (item: CartItem) => void;
  removeItem: (index: number) => void;
  updateQuantity: (index: number, quantity: number) => void;
  clearCart: () => void;
  totalAmount: () => number;
  totalItems: () => number;
}
```

```typescript
// src/stores/uiStore.ts
interface UiStore {
  isVendingMode: boolean;
  setVendingMode: (isVendingMode: boolean) => void;
  // ... 기타 클라이언트 UI 상태 (에러 모달 등)
}
```

```typescript
// src/stores/languageStore.ts
interface LanguageStore {
  currentLanguage: string;    // "ko", "en", "ja", ...
  supportedLanguages: string[];
  setLanguage: (lang: string) => void;
  setSupportedLanguages: (langs: string[]) => void;
}
```

### 6.2 서버 상태

서버 데이터(메뉴, 카테고리 등)는 fetch를 통해 조회하고 React의 useState/useEffect로 관리한다. 구매 페이지에서는 Next.js의 Server Components를 활용하여 초기 메뉴 데이터를 서버에서 렌더링한다.

---

## 7. Error Handling

### 7.1 Error Response Format

```json
{
  "error": {
    "code": "MENU_NOT_FOUND",
    "message": "메뉴를 찾을 수 없습니다."
  }
}
```

### 7.2 Error Code Definition

| Code | HTTP Status | Message | Handling |
|------|-------------|---------|----------|
| VALIDATION_ERROR | 400 | 입력값이 올바르지 않습니다 | 폼 에러 표시 |
| NOT_FOUND | 404 | 리소스를 찾을 수 없습니다 | 404 페이지 또는 toast |
| GEMINI_API_ERROR | 502 | AI 서비스에 일시적 오류가 발생했습니다 | toast + 재시도 버튼 |
| UPLOAD_ERROR | 500 | 파일 업로드에 실패했습니다 | toast + 재시도 |
| SERVER_ERROR | 500 | 서버 오류가 발생했습니다 | toast |

### 7.3 클라이언트 에러 처리

- API 호출 실패 시 toast 알림 (관리 페이지)
- 구매 페이지에서는 심플한 에러 메시지 모달
- Gemini API 실패 시: "AI 기능을 사용할 수 없습니다. 직접 입력해주세요." 안내

---

## 8. Security Considerations

- [x] Input validation: Prisma 파라미터 바인딩으로 SQL Injection 방지
- [x] File upload: 이미지 파일만 허용 (mime type 검증), 파일 크기 제한 (5MB)
- [ ] HTTPS enforcement: 배포 환경에서 설정 (초기 개발은 HTTP)
- [x] XSS prevention: React 기본 이스케이프 + dangerouslySetInnerHTML 미사용
- [x] Gemini API Key: 서버 사이드에서만 사용 (환경변수, 클라이언트 노출 없음)

> 인증/권한 시스템은 Out of Scope. 관리 페이지는 URL로 직접 접근 가능 (초기 구현).

---

## 9. Clean Architecture

### 9.1 Layer Structure

| Layer | Responsibility | Location |
|-------|---------------|----------|
| **Presentation** | UI 컴포넌트, 페이지, 레이아웃 | `src/app/`, `src/components/` |
| **Application** | 비즈니스 로직, 데이터 조합 | `src/app/api/` (API Routes) |
| **Domain** | 타입 정의, 공용 인터페이스 | `src/types/` |
| **Infrastructure** | Prisma Client, Gemini Client | `src/lib/` |
| **State** | 클라이언트 상태 관리 | `src/stores/` |

### 9.2 This Feature's Layer Assignment

| Component | Layer | Location |
|-----------|-------|----------|
| Shop/Admin Pages | Presentation | `src/app/shop/`, `src/app/admin/` |
| UI Components | Presentation | `src/components/shop/`, `src/components/admin/` |
| API Route Handlers | Application | `src/app/api/` |
| Type Definitions | Domain | `src/types/` |
| Prisma Client | Infrastructure | `src/lib/prisma.ts` |
| Gemini Client | Infrastructure | `src/lib/gemini.ts` |
| Cart/Language Store | State | `src/stores/` |

---

## 10. Coding Conventions

### 10.1 Naming Conventions

| Target | Rule | Example |
|--------|------|---------|
| Components | PascalCase | `MenuCard`, `CartBar` |
| Functions | camelCase | `getMenuById()`, `handleAddToCart()` |
| Constants | UPPER_SNAKE_CASE | `MAX_FILE_SIZE`, `DEFAULT_LANGUAGE` |
| Types/Interfaces | PascalCase | `MenuItem`, `CartItem` |
| Component files | PascalCase.tsx | `MenuCard.tsx` |
| Utility files | camelCase.ts | `prisma.ts`, `gemini.ts` |
| Folders | kebab-case | `generate-image/`, `option-groups/` |

### 10.2 API Route Convention

```
src/app/api/{resource}/route.ts          → GET (list), POST (create)
src/app/api/{resource}/[id]/route.ts     → GET (detail), PUT (update), DELETE
```

### 10.3 Environment Variables

| Variable | Purpose | Scope |
|----------|---------|-------|
| `DATABASE_URL` | `file:./dev.db` | Server |
| `GEMINI_API_KEY` | Gemini API 인증 | Server |

---

## 11. Implementation Guide

### 11.1 File Structure

```
k/
├── src/
│   ├── app/
│   │   ├── layout.tsx                    # Root layout
│   │   ├── page.tsx                      # / → /shop 리다이렉트
│   │   ├── shop/
│   │   │   ├── layout.tsx                # Shop layout (헤더, 하단바)
│   │   │   ├── page.tsx                  # 메뉴 조회 메인
│   │   │   └── order-complete/
│   │   │       └── page.tsx              # 주문 완료 화면
│   │   ├── admin/
│   │   │   ├── layout.tsx                # Admin layout (사이드바)
│   │   │   ├── page.tsx                  # Admin 메인 (→ 주문 관리)
│   │   │   ├── orders/
│   │   │   │   └── page.tsx              # 주문 관리
│   │   │   ├── categories/
│   │   │   │   └── page.tsx              # 카테고리 관리
│   │   │   ├── menus/
│   │   │   │   ├── page.tsx              # 메뉴 목록
│   │   │   │   ├── new/
│   │   │   │   │   └── page.tsx          # 메뉴 등록
│   │   │   │   └── [id]/
│   │   │   │       └── edit/
│   │   │   │           └── page.tsx      # 메뉴 수정
│   │   │   ├── translations/
│   │   │   │   └── page.tsx              # 번역 관리
│   │   │   └── settings/
│   │   │       └── page.tsx              # 매장 설정
│   │   ├── api/
│   │   │   ├── categories/
│   │   │   │   ├── route.ts              # GET, POST
│   │   │   │   └── [id]/
│   │   │   │       └── route.ts          # PUT, DELETE
│   │   │   ├── menus/
│   │   │   │   ├── route.ts              # GET, POST
│   │   │   │   └── [id]/
│   │   │   │       ├── route.ts          # GET, PUT, DELETE
│   │   │   │       └── options/
│   │   │   │           ├── route.ts      # POST (옵션그룹 생성)
│   │   │   │           └── [groupId]/
│   │   │   │               └── route.ts  # PUT, DELETE
│   │   │   ├── orders/
│   │   │   │   ├── route.ts              # GET, POST
│   │   │   │   └── [id]/
│   │   │   │       └── route.ts          # PATCH (상태 변경)
│   │   │   ├── translate/
│   │   │   │   └── route.ts              # POST
│   │   │   ├── generate-image/
│   │   │   │   └── route.ts              # POST
│   │   │   ├── tossfront/
│   │   │   │   └── route.ts              # GET (Mock)
│   │   │   └── settings/
│   │   │       └── route.ts              # GET, PUT
│   │   └── globals.css
│   ├── components/
│   │   ├── ui/                           # 공용 기본 컴포넌트
│   │   │   ├── Button.tsx
│   │   │   ├── Modal.tsx
│   │   │   └── Toast.tsx
│   │   ├── shop/                         # 구매 전용
│   │   │   ├── CategoryTabs.tsx
│   │   │   ├── MenuGrid.tsx
│   │   │   ├── MenuCard.tsx
│   │   │   ├── OptionModal.tsx
│   │   │   ├── CartBar.tsx
│   │   │   ├── CartView.tsx
│   │   │   ├── PaymentModal.tsx
│   │   │   ├── SignaturePad.tsx
│   │   │   ├── OrderComplete.tsx
│   │   │   └── LanguageSelector.tsx
│   │   └── admin/                        # 관리 전용
│   │       ├── AdminSidebar.tsx
│   │       ├── CategoryManager.tsx
│   │       ├── MenuManager.tsx
│   │       ├── MenuForm.tsx
│   │       ├── ImageUploader.tsx
│   │       ├── ImageGenerator.tsx
│   │       ├── TranslationManager.tsx
│   │       └── SettingsForm.tsx
│   ├── lib/
│   │   ├── prisma.ts                     # Prisma client singleton
│   │   └── gemini.ts                     # Gemini API client
│   ├── stores/
│   │   ├── cartStore.ts                  # 장바구니 Zustand store
│   │   └── languageStore.ts              # 언어 Zustand store
│   └── types/
│       └── index.ts                      # 공용 타입 정의
├── prisma/
│   └── schema.prisma
├── public/
│   ├── uploads/                          # 업로드 이미지
│   └── generated/                        # AI 생성 이미지
├── .env
├── package.json
├── tsconfig.json
├── tailwind.config.ts
└── next.config.ts
```

### 11.2 Implementation Order

1. [ ] **프로젝트 초기화**: Next.js + TypeScript + Tailwind 프로젝트 생성
2. [ ] **DB 설정**: Prisma 설치, schema.prisma 작성, 마이그레이션
3. [ ] **인프라 레이어**: `lib/prisma.ts`, `lib/gemini.ts`, 타입 정의
4. [ ] **API Routes (기본 CRUD)**: categories, menus, orders, settings
5. [ ] **API Routes (AI)**: translate, generate-image
6. [ ] **API Routes (Mock)**: tossfront
7. [ ] **Stores**: cartStore, languageStore
8. [ ] **공용 UI 컴포넌트**: Button, Modal, Toast
9. [ ] **관리 페이지**: Admin layout → 카테고리 → 메뉴(+이미지) → 번역 → 설정
10. [ ] **구매 페이지**: Shop layout → 메뉴 그리드 → 옵션 모달 → 장바구니 → 결제Mock → 주문완료
11. [ ] **다국어 적용**: 구매 페이지에 언어 전환 기능 통합
12. [ ] **연령 인식 및 자판기 모드**: 카메라 캡처 로직 추가 (`/api/estimate-age`), Zustand 연동 및 Vending UI 분기
13. [ ] **접근성 검증**: 글씨 크기, 터치 영역, 대비 확인

---

## Version History

| Version | Date | Changes | Author |
|---------|------|---------|--------|
| 0.1 | 2026-02-28 | Initial draft | dgoon |
